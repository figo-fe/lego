import React, { useState, useEffect } from 'react';
import { Wrap, Charts } from '../../components';
import { CHART } from '../../config/apis';
import { axios } from '../../common/utils';

export const BoardUse = props => {
  return (
    <Wrap>
      <div className='lego-card'></div>
    </Wrap>
  );
};
